<header class="navbar navbar-default">
  <div class="container">
    <nav>
      <ul class="nav navbar-nav navbar-left">
        <%= link_to image_tag('Logo.jpg', :alt=>'Un Aventon!', size: '50x50'), root_path %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Inicio", root_path %></li>
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to 'Perfil', current_user %>
                <%= link_to 'Cerrar sesión', destroy_user_session_path, method: :delete %>
              </li>
            </ul>
        <% else %>
          <li><%= link_to 'Crear cuenta', new_user_registration_path %></li>
          <li><%= link_to 'Iniciar sesión', new_user_session_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
