Correcciones y Bugs:

Correcciones de los ayudantes:
	~~-Cambiar lenguaje de errores de rails (change languaje rails errors)~~
	~~-Cambiar lenguaje de errores de devise (devise yml (yaml))~~
	~~-Agregar grid-system en application (columnas de Bootstrap)~~
	~~-Cambiar el formato del footer a footer de bootstrap (footer navbar)~~
	~~-Deshabilitar o quitar botones que no funcionan (ver viajes, crear viaje)~~
	~~-Recordar: un usuario puede tener una sola vez el mismo auto.~~

Bugs:
  - Las calificaciones se guardan en la timezone de UTC, si no se puede arreglar va a haber que restarle tres horas siempre manualmente.
